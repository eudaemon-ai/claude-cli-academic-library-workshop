AWS_REGION=us-east-1
# IAM Role Session — the role to assume for DynamoDB access
AWS_ROLE_ARN=arn:aws:iam::123456789012:role/LibraryWorkshopRole
AWS_ROLE_SESSION_NAME=library-workshop-session
# The credentials used to CALL STS AssumeRole come from the ambient credential
# chain: EC2/ECS instance profile, ~/.aws/credentials named profile, etc.
# No static access keys are required or expected.
DYNAMODB_TABLE=LibraryWorkshop
FACILITATOR_TOKEN=changeme
PUBLIC_WORKSHOP_TITLE=Library AI Workshop
PUBLIC_COHORT=spring2026
